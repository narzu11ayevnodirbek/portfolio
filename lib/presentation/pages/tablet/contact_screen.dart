import 'package:flutter/material.dart';

class ContactScreenTablet extends StatelessWidget {
  const ContactScreenTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}